import { Quiz } from "@/types/types";
import { question } from "@/mock/questions";

export const quiz: Quiz = {
  id: "892829",
  name: "html",
  description: "Preguntas sobre html",
  questions: [question, question, question, question, question],
  count: 0,
};
